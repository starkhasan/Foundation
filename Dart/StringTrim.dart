void main(){
  var str = '           Ali Hasan              ';
  print(str);
  /*
   * trim() function is used to remove the leading and trailing space of the string
   * trim() function also return the String
   */
  str = str.trim();
  print(str);
}
